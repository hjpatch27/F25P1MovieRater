import java.util.HashMap;
/**
 * Implementation of a sparse matrix. This will be used as the data structure
 * for our MovieRater. 
 * 
 * @author Henry Patch (hjpatch27), Nyssa Loeu (nyssal23)
 * @version 9.7.2025
 */
public class SparseMatrix 
{
    private int rows;
    private int cols;
    private HashMap<Integer, HashMap<Integer, Integer>> matrixElements;
}

/**
 * Inserts a given value into the sparse matrix at a specified
 * row and column.
 * @param row is the row number for the value to be inserted at.
 * @param col is the column number for the value to be inserted at.
 * @param value is the integer to be inserted into the spares matrix.
 */
public void insert(int row, int col, int value)
{
    
}

/**
 * 
 * @param row is the row number to access in the sparse matrix.
 * @param col is the column number to access in the sparse matrix.
 * @return int which is the value in the given row and column
 * of the sparse matrix.
 */
public int get(int row, int col)
{
    
}
